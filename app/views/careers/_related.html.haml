- unless @careers.nil?
  .filter-title
    Careers
    %ul
    - @careers.each do |career|
      %li= link_to career.title, :job_searches_keyword_contains => career.title, :job_searches_location_contains => params[:near]
    .filter-footer
      = link_to 'Select careers...', careers_path
