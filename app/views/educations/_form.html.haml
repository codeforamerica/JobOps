= form_for(@education) do |f|
  - if @education.errors.any?
    #error_explanation
      %h2= "#{pluralize(@education.errors.count, "error")} prohibited this education from being saved:"
      %ul
        - @education.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :school_name
    = f.text_field :school_name

  .field
    = f.label :degree
    = f.text_field :degree

  .field
    = f.label :study_field
    = f.text_field :study_field

  .field
    = f.label :start_date
    = f.date_select :start_date,
    :start_year => Time.now.year - 50,
    :discard_day => true,
    :discard_month => true,
    :include_blank => true

  .field
    = f.label :end_date
    = f.date_select :end_date,
    :start_year => Time.now.year - 50,
    :discard_day => true,
    :discard_month => true,
    :include_blank => true

  .field.activities
    = f.label :activities
    = f.text_area :activities
  %br.clear
  .field.notes
    = f.label :notes
    = f.text_area :notes

  .actions
    =f.submit "Submit", :value=>"Save", :class => "green-btn"

